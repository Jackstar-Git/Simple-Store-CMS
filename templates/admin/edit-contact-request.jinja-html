<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Admin Panel - Kontaktanfrage bearbeiten</title>
    <link rel="shortcut icon" href="{{ url_for('internal.uploads', filename='Logo.png') }}" type="image">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/admin/edit-contact-request.css') }}">
</head>
<body>
    <div class="main-container">
        {% include "admin/sidebar.jinja-html" %}

        <div class="main-content">
            <h2>Kontaktanfrage bearbeiten</h2>

            <form method="post" class="edit-contact-form">
                <input type="hidden" id="csrf_token" name="csrf_token" value="{{ generate_token() }}">
                <div class="form-group">
                    <label for="email" class="info-label">Email:</label>
                    <p id="email" class="info">{{ contact.email }}</p>
                </div>
                <div class="form-group">
                    <label for="name" class="info-label">Name:</label>
                    <p id="name" class="info">{{ contact.name }}</p>
                </div>
                <div class="form-group">
                    <label for="subject" class="info-label">Betreff:</label>
                    <p id="subject" class="info">{{ contact.subject }}</p>
                </div>
                <div class="form-group">
                    <label for="message" class="info-label">Nachricht:</label>
                    <p id="message" class="info">{{ contact.message }}</p>
                </div>
                <div class="form-group">
                    <label for="date" class="info-label">Datum:</label>
                    <p id="date" class="info">{{ contact.date }}</p>
                </div>
                <div class="form-group">
                    <label for="status">Status:</label>
                    <select name="status" id="status">
                        <option value="offen" {% if contact.status == 'offen' %}selected{% endif %}>Offen</option>
                        <option value="in bearbeitung" {% if contact.status == 'in bearbeitung' %}selected{% endif %}>In Bearbeitung</option>
                        <option value="geschlossen" {% if contact.status == 'geschlossen' %}selected{% endif %}>Geschlossen</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="notiz">Notiz:</label>
                    <textarea name="notiz" id="notiz">{{ contact.notiz }}</textarea>
                </div>
                <div class="form-actions">
                    <div class="left-actions">
                        <button type="submit" class="btn submit-btn"><i class="fas fa-save"></i> Speichern</button>
                        <a href="mailto:{{ contact.email }}?subject=Antwort auf Ihre Anfrage: {{ contact.subject }}" class="btn antworten-btn"><i class="fas fa-reply"></i> Antworten</a>
                    </div>
                    <a href="/admin/anfragen/kontakt" class="btn cancel-btn" onclick="return confirm('Bist du sicher, dass du abbrechen willst? Alle Änderungen gehen dabei verloren!')">Abbrechen</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
